let petName = 'Luxy';

// Rules
// 1. You can't define a variable more than once.
// 2.  There are rules when defining variable names

// Temp Converter
// Create a var for fahrenheit

// Calc celsius and store in celsius variable
// Print that value

// Calc celsius and store in celsius variable
// Print that value

let fahrenheit = 30;
let celsius = (fahrenheit - 32) * 5/9;
console.log(celsius);

let kelvin = (fahrenheit - 32) * 5/9 + 273.15;
console.log(kelvin);
